import App from './App.svlt'

export default ({ asr }) => {
	asr.addState({
		name: 'app',
		route: '/app',
		template: App,
		// TODO if user is not "logged in" take through onboarding flow
		defaultChild: 'log',
		resolve: async (data, parameters) => {
			// do a fetch of all the core account data
			return {
				hello: 'world'
			}
		}
	})
}
