import Log from './Log.svlt'

export default ({ asr }) => {
	asr.addState({
		name: 'app.log',
		route: '/log',
		template: Log,
		resolve: async (data, parameters) => {
			// do a fetch of minimal data
			return {
				hello: 'world'
			}
		},
		activate({ domApi }) {
			// fetch more data and update
		}
	})
}
