import Configure from './Configure.svelte'

export default ({ asr }) => {
	asr.addState({
		name: 'app.configure',
		route: '/configure',
		template: Configure,
		resolve: async (data, parameters) => {
			// do a fetch of minimal data
			return {
				hello: 'world'
			}
		},
		activate({ domApi }) {
			// fetch more data and update
		}
	})
}
