import Edit from './Edit.svelte'

export default ({ asr }) => {
	asr.addState({
		name: 'app.configure.container.edit',
		route: '/edit/:id',
		template: Edit,
		resolve: async (data, parameters) => {
			return {
				container: {}
			}
		},
		activate({ domApi }) {
			// fetch more data and update
		}
	})
}
