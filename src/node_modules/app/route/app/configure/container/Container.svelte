<script>
import Card from 'app/element/Card.svelte'

export let asr = {}
export let containerList = []
</script>

<style>
img {
	max-width: 100%;
	max-height: 5rem;
}
</style>

<h3>
	Containers
</h3>

{#each containerList as container, index (container.id)}
	<Card>
		<div slot="header">
			{container.name}
			-
			{container.volume}oz
		</div>
		<div slot="body">
			{#if container.image}
				<img src={container.image} alt="Image of {container.name} container.">
			{/if}
			<p>
				<a href="{ asr.makePath('app.configure.container.edit', { id: container.id }) }">
					Edit Container
				</a>
			</p>
		</div>
	</Card>
{/each}
