import History from './History.svlt'

export default ({ asr }) => {
	asr.addState({
		name: 'app.history',
		route: '/history',
		template: History,
		resolve: async (data, parameters) => {
			// do a fetch of minimal data
			return {
				hello: 'world'
			}
		},
		activate({ domApi }) {
			// fetch more data and update
		}
	})
}
